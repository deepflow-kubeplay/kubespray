---
- name: Chrony | restart chrony service
  service:
    name: "{{ chrony_service_name }}"
    state: restarted
    enabled: yes

- name: buildkit | restarted buildkit service
  systemd:
    name: buildkit
    enabled: yes
    state: restarted
  when: buildkit_enabled | bool
    